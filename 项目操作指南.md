# 个人作品集网站 — 完整操作指南

---

## 一、项目概览

这是一个**个人作品集网站**，已经部署在 GitHub Pages 上，任何人都可以通过网址访问。

| 项目信息 | 详情 |
|----------|------|
| 线上地址 | **https://lss123159-lang.github.io/portfolio/** |
| 代码仓库 | **https://github.com/lss123159-lang/portfolio** |
| 本地路径 | `C:\Users\12094\Desktop\deepseek project` |
| 技术栈 | 纯 HTML + CSS + JavaScript，无框架依赖 |

---

## 二、系统架构（给以后的我 / Claude 看）

### 文件结构

```
deepseek project/
├── index.html          # 主网站页面（开发版，内容从 config.json 动态加载）
├── admin.html          # 管理面板（在浏览器里改内容、换配色）
├── config.json         # 核心配置：所有文字内容 + 颜色主题，集中管理
├── server.js           # 本地开发服务器（Node.js，80行，读写 config.json）
├── build.js            # 打包脚本：把 config.json 的内容"烤"进静态 HTML
├── package.json        # 项目配置 + 命令脚本
├── css/
│   └── style.css       # 所有样式（CSS 变量驱动配色，响应式适配）
├── js/
│   └── main.js         # 前端逻辑（加载配置、渲染页面、滚动动画、导航交互）
├── images/             # 作品截图等图片资源
└── docs/               # 【核心】静态打包输出目录，GitHub Pages 直接从这个目录读取
    └── index.html      # 自包含的静态网站文件（CSS+JS+内容全在一个文件里）
```

### 数据流

```
[你] → admin.html → server.js → config.json → build.js → docs/index.html → GitHub Pages → [访问者]
```

1. **你在管理面板改内容** → 数据 POST 到 server.js
2. **server.js 写入 config.json** → 内容持久化保存
3. **运行 npm run build** → build.js 读取 config.json，把所有内容"烤"进 docs/index.html
4. **git push** → docs/ 推上 GitHub
5. **GitHub Pages 自动更新** → 访问者看到新内容

### 设计系统

- **配色**：9 个颜色变量（见 `config.json` → `theme`），CSS 变量驱动，管理面板可视化修改
- **布局**：单页滚动，5 个板块（Hero / 作品 / 关于 / 联系 / Footer）
- **排版**：系统字体栈，中文优先 PingFang SC / Microsoft YaHei
- **响应式**：768px（平板）、480px（手机）两个断点
- **动画**：Intersection Observer 触发滚动渐入 + 卡片 hover 微交互
- **导航**：固定顶部，滚动后毛玻璃效果（color-mix + backdrop-filter）

---

## 三、日常操作（Step by Step）

### 场景 A：修改网站内容或配色

> 你需要做的事情：启动本地服务器 → 打开管理面板 → 修改 → 保存 → 打包 → 推送上线

**Step 1：打开终端**

- 按 `Win + R`，输入 `cmd`，回车
- 或者直接在 Windows 搜索栏搜"终端"或"PowerShell"

**Step 2：进入项目目录**

```
cd "C:\Users\12094\Desktop\deepseek project"
```

**Step 3：启动本地服务器**

```
npm start
```

看到以下提示说明启动成功：

```
Portfolio 管理后台已启动
网站:     http://localhost:3000
管理面板: http://localhost:3000/admin.html
```

**Step 4：打开管理面板**

- 打开浏览器，地址栏输入 `http://localhost:3000/admin.html`
- 左侧有 7 个标签页，分别管理不同区域：
  - **配色主题**：9 个颜色选择器，点色块修改，实时生效
  - **导航设置**：Logo 文字、导航链接（可增删改排序）
  - **Hero 区**：首页大标题、名字、介绍语
  - **作品展示**：项目卡片，每个卡片包含标签、标题、描述、图片路径
  - **关于页面**：技能标签（逗号分隔）、经历时间线
  - **联系方式**：邮箱、GitHub 等链接
  - **站点设置**：网页标题、SEO 描述、Footer 文字

**Step 5：修改内容**

- 修改完成后，点页面右上角的 **「保存更改」** 按钮
- 或直接按 `Ctrl + S`
- 看到右下角弹出"已保存"提示即成功

**Step 6：预览效果**

- 新开一个浏览器标签页，访问 `http://localhost:3000`
- 查看修改后的效果，不满意就回管理面板继续改

**Step 7：打包并推送上线**

修改满意后，回到终端，输入：

```
npm run deploy
```

这一条命令会自动完成三件事：
1. 把最新内容打包成静态网站（build.js 读取 config.json → 写入 docs/index.html）
2. Git 提交变更
3. 推送到 GitHub

等几秒钟，线上网站 `https://lss123159-lang.github.io/portfolio/` 就会更新。

**Step 8：关闭服务器**

在终端里按 `Ctrl + C` 停止服务器。

---

### 场景 B：添加作品截图

1. 把图片文件放到 `C:\Users\12094\Desktop\deepseek project\images\` 目录下
2. 按场景 A 的步骤启动服务器，打开管理面板
3. 在「作品展示」标签页，找到对应项目卡片的「图片路径」输入框
4. 填入 `images/你的文件名.jpg`（例如 `images/project1.jpg`）
5. 保存 → `npm run deploy`

---

### 场景 C：只改小内容，快速上线

如果你不想打开管理面板（比如只改一个字），可以直接编辑 `config.json`：

1. 用记事本打开 `C:\Users\12094\Desktop\deepseek project\config.json`
2. 找到要改的字段，直接修改文字
3. 保存文件
4. 终端运行 `npm run deploy`

---

## 四、命令速查表

| 命令 | 作用 | 什么时候用 |
|------|------|-----------|
| `npm start` | 启动本地服务器 | 每次要改内容之前 |
| `npm run build` | 打包静态网站到 docs/ | 只想打包，不推送 |
| `npm run deploy` | 打包 + 推送上线 | 改完内容要发布时 |
| `node server.js` | 同上（npm start 的完整写法） | npm start 不行时用这个 |
| `node build.js` | 同上（npm run build 的完整写法） | npm run build 不行时用这个 |

---

## 五、GitHub 相关操作

### 查看仓库状态

```
git status
```

### 查看提交历史

```
git log --oneline
```

### 如果 deploy 命令因为 git 冲突失败

```
git pull origin master
```

然后重新 `npm run deploy`

### 如果想把整个项目搬到另一台电脑

```
git clone https://github.com/lss123159-lang/portfolio.git
cd portfolio
```

然后就可以在新电脑上用 `npm start` → 管理面板 → `npm run deploy` 的流程了。

---

## 六、故障排查

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| `npm start` 报错 | 可能端口 3000 被占用 | 换个端口：修改 server.js 第 4 行 `PORT = 3000` 为其他数字，如 `3001` |
| 管理面板保存失败 | server.js 没启动 | 确认终端里 `npm start` 还在运行 |
| `npm run deploy` 报错 | 网络问题或 GitHub 登录过期 | 终端运行 `gh auth status` 检查登录状态，如果过期：`gh auth login` |
| 网站打开是空白 | 可能是直接双击了 index.html | 应该访问 `http://localhost:3000`（开发时）或线上地址 |
| 推上去后网站没更新 | GitHub Pages 需要几十秒刷新 | 等 1-2 分钟，或清除浏览器缓存（Ctrl+F5） |

---

## 七、如果以后想找人继续改进

把这个文档给开发者看，对方一看就明白整个架构。核心要点：

1. **内容全在 config.json**，改它就能改网站内容
2. **配色全在 config.json → theme**，9 个字段对应 9 个 CSS 变量
3. **admin.html 是管理面板**，可视化编辑，不需要懂代码
4. **build.js 是打包脚本**，把 config.json 的内容"编译"成纯静态 HTML
5. **docs/ 是部署目录**，GitHub Pages 直接从这个目录读取
6. **线上地址**：`https://lss123159-lang.github.io/portfolio/`

---

> 最后更新：2026 年 5 月 16 日
> GitHub 用户名：lss123159-lang
> 注册邮箱：lss123159@gmail.com
